export interface DisplayProps {
  /**
   * Animation key used to pull from local storage
   **/
  animationKey: string;
}

export interface DisplayRatioProps {
  artboardWidth: number;
  artboardHeight: number;
}

export interface DisplaySizeProps {
  artboardWidth: number;
  artboardHeight: number;
  width: number;
}
