export interface AspectRatioProps {
  parentWidth: number;
  parentHeight: number;
  artboardWidth: number;
  artboardHeight: number;
}

export interface ArtboardSizeProps {
  artboardWidth: number;
  artboardHeight: number;
  scale: number;
}
