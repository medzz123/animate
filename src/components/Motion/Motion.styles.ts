import styled from 'styled-components';

import { tokens } from '../../theme/tokens';

export const MinLayoutContainer = styled.div`
  /* min-width: calc(100vw - ${tokens.sizes[48]}); */
  /* min-height: calc(100vh - 6.813rem - ${tokens.sizes[24]}); */
`;
